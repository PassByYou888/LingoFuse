Metadata-Version: 2.4
Name: lingofuse
Version: 1.0.0
Summary: Python bindings for the LingoFuse RPC framework
Home-page: https://github.com/PassByYou888/LingoFuse
Author: passbyyou888 / Team
Author-email: 600585@qq.com
Classifier: Programming Language :: Python :: 3
Classifier: License :: OSI Approved :: MIT License
Classifier: Operating System :: OS Independent
Requires-Python: >=3.7
Description-Content-Type: text/markdown
Provides-Extra: bridge
Requires-Dist: Flask>=2.0; extra == "bridge"
Requires-Dist: requests>=2.25; extra == "bridge"
Provides-Extra: dev
Requires-Dist: pytest>=7.0; extra == "dev"
Requires-Dist: black; extra == "dev"
Requires-Dist: flake8; extra == "dev"
Dynamic: author
Dynamic: author-email
Dynamic: classifier
Dynamic: description
Dynamic: description-content-type
Dynamic: home-page
Dynamic: provides-extra
Dynamic: requires-python
Dynamic: summary

# LingoFuse Python 绑定

> **“当你的 Python 函数开始被十几种语言排队调用，而你还不知道发生了什么——恭喜，你已跨入智能体时代。”**


## 这玩意儿到底能干啥？

想象一下：你用 Python 写了一个函数，正愁怎么让 C++ 的同事调用、怎么让 Go 的服务消费、怎么让浏览器里的 JS 点一下按钮就拿到结果……

**LingoFuse 一拍桌子：别愁了，全包了。**

你唯一需要做的，就是给函数戴个装饰器帽子 `@expose`。然后——C++ 能调，Go 能调，Rust 能调，Java、C#、Pascal、Node.js、PHP，甚至你奶奶的浏览器都能调。

**就这么简单？对，就这么简单。**

> **LingoFuse 不是什么花里胡哨的新协议，而是一张让所有编程语言“互捅”的神经通讯网。**


## 🏗️ 原理？——其实不用懂太多

```mermaid
flowchart LR
    A["你写的 Python 函数<br/>@expose 装饰器"] --> B["LingoFuse 核心<br/>(C4 网格)"]
    B --> C["Python 客户端"]
    B --> D["C++ 客户端"]
    B --> E["Go 客户端"]
    B --> F["Rust/Java/C#"]
    B --> G["浏览器/Node/PHP"]
```

但如果你非要懂——那大概就是：你的 Python 函数注册进了一个叫 **C4 服务网格** 的东西，然后全世界都能通过 **二进制协议** 找到它，再也不用写 REST API、不用折腾 Protobuf、不用发愁跨语言序列化。


## ⚡ 性能？——你的 Python 能跑多快，它就能跑多快（甚至更快）

| 方案 | 延迟 | 吞吐 | 服务发现？ | 负载均衡？ | 顺序保证？ |
|------|------|------|------------|------------|------------|
| LingoFuse (IPC) | **< 1 ms** | **12k+/s** | ✅ 内置 | ✅ 内置 | ✅ FIFO |
| LingoFuse (TCP) | **~2.5 ms** | **6.5k+/s** | ✅ 内置 | ✅ 内置 | ✅ FIFO |
| gRPC | ~5-8 ms | ~3k/s | ❌ 要 etcd | ❌ 要 LB | ❌ |
| REST | ~10-20 ms | ~1k/s | ❌ 要 Nginx | ❌ 要 Nginx | ❌ |
| MQ | ~5-15 ms | ~2k/s | ❌ 要 Broker | ❌ 要配置 | ⚠️ 需额外配置 |

**翻译成人话：** LingoFuse 在 IPC 模式下比 gRPC 快 **5-8 倍**，比 REST 快 **10-20 倍**，并且自带服务发现、负载均衡、断线重连和顺序保证——这些东西在别的方案里都得你自己拼积木，**LingoFuse 直接送全套**。

**为什么这么猛？** 因为它不玩 JSON，不玩 HTTP，不玩 Protobuf 序列化——直接**零拷贝二进制传输**，回调在 C 层线程池调度，绕过了 Python GIL 对并发的限制。


## 🚀 上手：三步拿下

### 第一步：装包 + 扔个动态库

```bash
pip install -e .
```

然后把动态库（`LingoFuse64.dll` / `liblingofuse.so`）丢到系统 PATH 或当前目录——**下载解压，完事。**

### 第二步：写个服务端（就 7 行）

```python
from lingofuse import Server

app = Server("Calc", "Just a calculator")

@app.expose("add")
def add(a, b):
    return a + b

app.start_multi(["ipc:calc", "0.0.0.0:9898"])
input("按回车跑路...\n")
app.stop()
```

### 第三步：写个客户端（比点外卖还快）

```python
from lingofuse import C4

c = C4("Calc", "ipc:calc")
print(c.add(10, 20))   # 30
```

**打完收工。** 你那个 `add` 函数现在已经挂到全球分布式网络上，谁都能调。

想更骚一点？直接起 HTTP 网关，让浏览器也来凑热闹：

```bash
python lingofuse/bridge.py --endpoint ipc:calc --app Calc --port 8081
```

然后浏览器里：
```javascript
fetch('http://127.0.0.1:8081/Calc/add', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: '[10,20]'
}).then(r => r.json()).then(console.log); // 30
```

**你看，前后端通吃，后端还不用写一行 HTTP 代码。**


## 🎯 核心亮点（专治各种“我调不动你”的毛病）

| 特性 | 有多野？ |
|------|----------|
| 🌍 **10+ 语言互调** | Python、Pascal、C++、Go、Rust、Java、C#、Node、PHP、Web.js——全语言通杀 |
| ⚡ **性能怪兽** | IPC 模式 < 1ms，吞吐 12k+/s，比 REST 快 10 倍 |
| 🔌 **双模通信** | TCP 跨机，IPC 同机，一个地址字符串搞定切换 |
| 🔄 **自动服务发现 & 负载均衡** | 节点上来就注册，请求自动洒向最闲的那个 |
| 📦 **零拷贝** | 数据不复制，直接指针传递，快到你怀疑人生 |
| 🎯 **Call + Notify** | 要响应还是要速度，你说了算 |
| 🔗 **Sequenced Notify** | FIFO 有序推送，大数据分片传输稳如老狗 |
| 🧹 **自动回收** | 数据句柄 5 分钟不用自动释放，7x24 不重启 |
| 🔧 **部署模式** | 节点无序启动，谁先来谁后到无所谓 |
| 🆔 **唯一化 AppName** | `generate_app_name()` 生成全球唯一 ID，点对点通信永不撞车 |
| 📊 **可观测性** | `check_app`、`get_status` 让你随时知道谁活着谁躺了 |
| 🌉 **HTTP 桥接** | 自带 Flask 网关，Web 生态无缝接入 |
| 🧠 **AI 友好** | 注释详实，喂给 AI 就能帮你写代码 |


## 🐝 来，看个活的：Cross Demo 负载均衡现场

你开 10 个终端，每个跑一个 `cross_node`，然后开 5 个 `cross_call` 疯狂发请求——**你会看到 10 个窗口同时刷屏，但每个窗口收到的请求数量差不多。**

这就是 C4 网格的 **自动负载均衡**，不需要你写一行调度代码。

```bash
# 终端1：注册中心
python cross/cross_service.py

# 终端2-11：开 10 个工作节点（随便开）
python cross/cross_node.py   # 重复执行

# 终端12-16：开 5 个客户端疯狂呼叫
python cross/cross_call.py   # 重复执行
```

然后你会看到请求像蜜蜂一样均匀飞到每个节点上——**真·蜂群分布式。**

配套的多语言客户端（Node.js、PHP、浏览器）都在 `cross/` 目录里，直接拿来就能跑。


## 🌉 HTTP 桥接：让你的服务被“万国”调用

LingoFuse 的 `bridge.py` 是一个独立 Flask 网关，专门伺候那些没法直接加载 C 库的“二等公民”（PHP、Node.js、浏览器）。

```bash
python lingofuse/bridge.py --endpoint ipc:cross --app demo --port 8081
```

然后：

- **PHP**：`curl` POST 到 `/demo/add`，带 JSON 数组。
- **Node.js**：`fetch` 一样搞。
- **浏览器**：点一下按钮，AJAX 搞定。

桥接器不碰你的数据，**纯二进制转发**，所以 JSON、Protobuf、MessagePack 随便你往里塞。


## 🔗 LLM 流式服务：把 ChatGPT 级别的生成塞进分布式管道

项目 `llm-service/` 里藏了一个大杀器：

- **服务端**：基于 `llama-cpp-python`，暴露 `generate` API，立即返回 `session_id`，然后后台流式生成 token，通过 `Sequenced_Notify` 推送给客户端。
- **客户端**：动态生成唯一名称，注册 Notify 接收器，实时打印 token 流。

**跑起来：**
```bash
# 装依赖
pip install -r llm-service/requirements.txt

# 启动服务（模型路径自己配）
python llm-service/llm_service.py --model-path ./qwen.gguf

# 另一个终端跑客户端
python llm-service/llm_test.py --content "print('Hello')" --prompt "解释"
```

你在客户端会看到文本一个字一个字地蹦出来——**这就是流式 LLM + 分布式 RPC 的浪漫。**


## 🧩 接口生成：AI 不写代码，AI 只做结构体

LingoFuse 的代码生成逻辑跟那些“AI 随缘写代码”的妖艳贱货不一样：

1. 你把 Python 代码（带 `@expose`）喂给 AI。
2. AI **不直接生成目标语言代码**，而是输出一份**结构化接口描述**（JSON/YAML）。
3. 然后我们的**确定性代码生成器**读取这份结构体，一键生成 Go、Rust、Java、C#、PHP、Node.js 的完整客户端——**零手工修改，直接编译运行。**

**为什么绕这么一圈？** 因为 AI 直接写代码你还要 debug，而结构体 + 生成器的组合**百分百可靠**，一次生成，永远能用。


## 📦 目录结构（一眼看穿）

```
Py/
├── lingofuse/              # 核心包，你 import 的地方
│   ├── __init__.py         # 导出版本号、主要类
│   ├── _lf_native.py       # ctypes 原始绑定（别碰，除非你懂 C）
│   ├── core.py             # DataHandle + App
│   ├── server.py           # Server + @expose
│   ├── client.py           # C4 动态客户端
│   ├── bridge.py           # HTTP 网关（独立运行）
│   ├── errors.py           # 自定义异常
│   └── serializers.py      # JSON 序列化
├── cross/                  # 负载均衡演示，全语言客户端
├── llm-service/            # 流式 LLM 服务 + 客户端
├── setup.py                # pip install 入口
└── README.md               # 就是你正在看的
```


## ❓ 你可能会踩的坑（我都帮你踩过了）

**Q：`generate_app_name()` 什么时候调？**  
A：**必须在 `PrepareDone()` 返回成功后调**，否则生成的名字不包含网络隧道 ID，会找不到路。

**Q：`check_app()` 说服务不在线，但我明明注册了？**  
A：冷静，那是缓存延迟，**广播要 3 秒左右**。要么等，要么直接调，要么重试。

**Q：回调里能调 `call` 吗？**  
A：**绝对不行！** 会死锁。想远程调用，另开线程去干。

**Q：想多个应用绑到同一个地址？**  
A：设 `Overlap_Connection=True`，然后反复 `PrepareClient`，每个 App 独享一条隧道。

**Q：HTTP 桥接返回的数据里有 `\0`，JSON 解析炸了？**  
A：新版 `bridge.py` 会自动剥离尾部 `\0`，放心用。如果自己写客户端，记得发请求时加上 `\0`（因为 Pascal 那边 `LF_ReadString` 等它）。

**Q：动态库找不到？**  
A：把 `LingoFuse64.dll` 扔到当前目录，或者加到 PATH。Windows 下跑 `init_env.ps1` 自动搞定。


## 🌐 更多资源

- **Pascal 绑定**：`../pascal/` —— 同样猛，适合老司机。
- **HTTP 桥接完整指南**：`lingofuse/Bridge_User_Guide.md`
- **LLM 流式要点**：`llm-service/LingoFuse_Python_Streaming_LLM_Guide.md`
- **Cross Demo 中文手册**：`cross/Cross_Demo_Guide_zh.md`
- **GitHub 首页**：[https://github.com/PassByYou888/LingoFuse](https://github.com/PassByYou888/LingoFuse)


## 🧓 关于作者

**老张（QQ: 600585）**  
看不惯跨语言调用得写一箩筐胶水代码，干脆撸了 LingoFuse。  
欢迎来撩、来喷、来 PR——**Star 是最好的催更。**


## 📄 许可证

**MIT**——拿去用，拿去改，拿去卖，都不用来谢我。


*文档最后更新于 2026 年 9 月，随 LingoFuse v3.0 发布。有问题提 Issue，急事加 Q。*
